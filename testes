const {Voos,Passagem, sequelize} = require('./models');

Passagem.findByPk(10).then(
    data => {
        console.log(data.toJSON());
        sequelize.close();
    }
)


Voos.findByPk(6).then(
    data => {
        console.log(data.toJSON());
        sequelize.close();
    }
);


